## Pattern: Adversarial (debate / red-team)

<!-- TODO: Write this pattern. Key idea: threads that must react to each
     other's findings. Requires phased execution — position threads run first,
     critic thread runs second with their episodes injected. Documents serve as
     the shared evidence base. -->
