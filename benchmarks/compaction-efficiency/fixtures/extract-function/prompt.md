In `handlers.py`, the `handle_create` and `handle_update` functions both contain duplicated validation logic (lines that check `if not data.get("name")` and `if len(data.get("name", "")) > 100`). Extract this repeated validation into a `validate_name(data: dict) -> str | None` function that returns the name if valid, or raises a `ValueError` with a descriptive message. Update both handlers to use it.